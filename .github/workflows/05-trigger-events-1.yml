name: trigger_event1

on:
  issues:
    types:
      - opened

jobs: 
  check_issue_created:
    runs-on: ubuntu-latest
    steps: 
      # 1er job pr tester
      - name: "afficher issue"
        run: echo "issue créée!"
  