name: trigger_event

on: workflow_dispatch

jobs: 
  check_issue_created:
    if: ${{github.issue.action == 'openened' }} 
    runs-on: ubuntu-latest
    steps: 
      # 1er job pr tester
      - name: "afficher issue"
        run: echo "issue créée"
  